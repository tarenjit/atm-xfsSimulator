{
  "name": "@atm/atm-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Next.js 14 ATM screen + operator console for the XFS simulator.",
  "scripts": {
    "dev": "rm -rf .next && next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "typecheck": "tsc --noEmit",
    "lint": "next lint --max-warnings 0 || true",
    "clean": "rm -rf .next .turbo node_modules/.cache",
    "test": "echo 'no tests yet' && exit 0"
  },
  "dependencies": {
    "@atm/xfs-core": "workspace:*",
    "clsx": "^2.1.1",
    "next": "^14.2.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "socket.io-client": "^4.7.5",
    "tailwind-merge": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.19",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.3",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3",
    "typescript": "^5.4.5"
  }
}
